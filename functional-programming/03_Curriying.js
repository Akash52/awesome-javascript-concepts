//Currying
// Currying simply means evaluating functions with multiple arguments and decomposing them into a sequence of functions with a single argument.

// function add(x) {
//   return function (y) {
//     return x + y;
//   };
// }

// const addThree = add(3);
// console.log(addThree(4));
// console.log(addThree(6));

//Arity

// 1 = unary -> Recieve one arguments
// 2 = binary -> Recieve two arguments
// 3 = ternary -> Recieve three arguments
// 4 = quaternary -> Recieve four arguments
